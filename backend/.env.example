#-------------------------------------------#
# Server Configuration
#-------------------------------------------#
NODE_ENV=development
PORT=3000
API_VERSION=v1

# CORS Origins (comma-separated)
CORS_ORIGIN=http://localhost:5173,http://localhost:3000,http://localhost:3001,http://localhost:3002

#-------------------------------------------#
# DATABASE - Supabase PostgreSQL (Optional)
# Chỉ cần mục này nếu backend kết nối PostgreSQL trực tiếp
# bằng Prisma, Drizzle, hoặc pg client.
# Nếu bạn chỉ dùng Supabase API -> KHÔNG cần phần này.
#-------------------------------------------#

# Kết nối PostgreSQL trực tiếp (Prisma / Drizzle / pg)
DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@db.<your-project-ref>.supabase.co:5432/postgres

# (Optional) Thông tin tách rời nếu bạn tự dùng pg client
DB_HOST=db.<your-project-ref>.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=YOUR_DB_PASSWORD

# Supabase PostgreSQL luôn yêu cầu SSL
DB_SSL=true


#-------------------------------------------#
# SUPABASE API (Quan trọng nhất)
# Đây là cái bạn CHẮC CHẮN cần dùng trong backend.
# - SUPABASE_URL: API endpoint
# - SUPABASE_SERVICE_ROLE_KEY: Key admin (server only)
#-------------------------------------------#
SUPABASE_URL=YOUR_SUPABASE_URL

# Service Role Key — chỉ dùng ở backend!
# ⚠️ Tuyệt đối không đưa lên frontend
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY


#-------------------------------------------#
# Redis (nếu dùng)
#-------------------------------------------#
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

#-------------------------------------------#
# JWT Authentication (của backend app)
# Không liên quan Supabase Auth
#-------------------------------------------#
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_ACCESS_EXPIRY=1h
JWT_REFRESH_EXPIRY=30d

#-------------------------------------------#
# Email Service
#-------------------------------------------#
EMAIL_SERVICE=smtp
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=noreply@codequest.com

#-------------------------------------------#
# AI Services
#-------------------------------------------#
OPENAI_API_KEY=your-openai-api-key
AI_MODEL=gpt-4
AI_MAX_TOKENS=2000

#-------------------------------------------#
# Code Execution
#-------------------------------------------#
DOCKER_EXECUTION_ENABLED=true
CODE_TIMEOUT_MS=5000
CODE_MEMORY_LIMIT_MB=128

#-------------------------------------------#
# Object Storage (AWS S3 hoặc MinIO)
#-------------------------------------------#
S3_BUCKET_NAME=codequest-storage
S3_REGION=us-east-1
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key
S3_ENDPOINT=https://s3.amazonaws.com

#-------------------------------------------#
# RabbitMQ
#-------------------------------------------#
RABBITMQ_URL=amqp://localhost:5672
QUEUE_NAME=codequest-tasks

#-------------------------------------------#
# Rate Limiting
#-------------------------------------------#
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

#-------------------------------------------#
# CORS
#-------------------------------------------#
CORS_ORIGIN=http://localhost:5173
CORS_CREDENTIALS=true

#-------------------------------------------#
# Logging
#-------------------------------------------#
LOG_LEVEL=info
LOG_FILE_PATH=./logs/app.log

#-------------------------------------------#
# WebSocket
#-------------------------------------------#
WS_PORT=3001
WS_PATH=/ws

# AI Configuration - Google Gemini
# Get new key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=YOUR_GEMINI_API_KEY
GEMINI_MODEL=YOUR_GEMINI
# Only gemini-pro and gemini-pro-vision work with v1beta API